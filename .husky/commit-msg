#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 🏢⚡ Energy Optimizer Pro - Commit Message Hook
# ===============================================

# Validate commit message format using commitlint
npx commitlint --edit "$1"
